name: Deploy Harmonix

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Build Docker Image
        run: docker build -t harmonix .

      - name: Run Container
        run: docker run -d -p 8000:8000 harmonix